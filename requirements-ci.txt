# Minimal deps for CI (no dashboard/report libs -> fast, cache-friendly builds)
duckdb>=1.1,<2.0
dbt-core>=1.8,<1.10
dbt-duckdb>=1.8,<1.10
great-expectations>=0.18,<0.19
pandas>=2.0
numpy>=1.24,<2.0
Faker>=24.0
python-dotenv>=1.0
tabulate>=0.9
pytest>=8.0

# ML serving tests (model artifacts are committed, so these run in CI)
fastapi>=0.110
httpx>=0.27
scikit-learn>=1.4
lightgbm>=4.0
joblib>=1.3
pyarrow>=14.0
